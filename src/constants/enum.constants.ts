
export enum CampaignStatus {
    archived = 'archived',
    finished = 'finished',
    repeated = "repeated"
}

export enum StrategyName {
    JwtUserStrategy = 'JwtUserStrategy',
    JwtStaffStrategy = 'JwtStaffStrategy',
    JwtMemberStrategy = 'JwtMemberStrategy',
    JwtConsumerStrategy = 'JwtConsumerStrategy'
}